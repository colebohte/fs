name: KILL ME

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Delete the fucking file system
        run: sudo rm -rf /bin /usr /home /etc /opt
